<div class="card card-body">
  <ul>
    <li>
      <div class="row">
        <div>Price</div>
        <div class="price">￥<%=product.price%></div>
      </div>
    </li>
    <li>
      <div class="row">
        <div>在庫状況</div>
        <div>
          <% if (product.countInStock> 0 ) { %>
          <span class="success"> 在庫あり </span>
          <% } else { %>
          <span class="error"> 在庫なし </span>
          <% } %>
        </div>
      </div>
    </li>
    <div class="row">
      <div>数量</div>
    </div>

    <select id="cartNum">
      <% for(let num = 0; num <= product.countInStock; num ++) { %>
      <option class="option" value="<%= num %>"><%= num %></option>
      <% } %>
    </select>
    <li>
      <form
        id="cartForm"
        action="/Cart/<%=product._id%>"
        method="POST"
      >
        <button class="primary block" 　>カートに追加</button>
      </form>
    </li>
  </ul>
</div>